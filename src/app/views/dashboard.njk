{% extends '_layouts/default.njk' %}

{% block body %}
  <div class="content">
    <strong>Olá, {{ user.name }} {% if user.provider %}<a href="/app/dashboard/appointments">Agenda</a>{% endif %}</strong>
    <p>Inicie um agendamento escolhendo um profissional abaixo:</p>

    <ul class="providers">
      {% for provider in providers %}
        <li class="provider">
          <div>
            <img src="/file/{{ provider.avatar }}" alt="Avatar">
            <strong>{{ provider.name }}</strong>
          </div>
          <a href="/app/appointments/new/{{ provider.id }}">+</a>
        </li>
      {% endfor %}
    </ul>
    <a href="/app/logout" class="logout">Sair</a>
  </div>
{% endblock %}